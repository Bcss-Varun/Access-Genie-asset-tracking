# Base URL the client calls. Left as a relative path in development so requests
# go through the Vite proxy (see vite.config.ts) and share the app's origin.
VITE_API_URL=/api/v1

# Where the dev proxy forwards /api — the Express server.
VITE_API_PROXY=http://localhost:4000
