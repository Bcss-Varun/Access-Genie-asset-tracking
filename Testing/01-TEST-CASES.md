# Asset Management — Test Case Register

Every case below is **automated and executed** — none are aspirational. Source:
`Testing/harness/api-tests.mjs`, `Testing/harness/registration-tests.mjs`
and `Testing/harness/ui-tests.mjs`.

Reproduce the whole suite:

```bash
node Testing/harness/api-tests.mjs
node Testing/harness/registration-tests.mjs
node --experimental-websocket Testing/harness/registration-ui-tests.mjs
node --experimental-websocket Testing/harness/ui-tests.mjs
node Testing/harness/report.mjs
```

**Preconditions for every case:** the API is up on `:4000` with a connected
database, the web app on `:5173`, and the seeded super-admin exists
(`npm run seed`). Each runner authenticates itself and deletes everything it
creates.

| Test ID | Module | Feature | Type | Priority | Steps | Expected Result | Actual Result | Status | Severity |
|---|---|---|---|---|---|---|---|---|---|
| `AM-API-001` | Asset Management | GET /assets returns success envelope with meta | API | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 200, {success:true, data:[], meta:{page,limit,total}} | 200, keys=success,data,meta, meta={"page":1,"limit":5,"total":2,"totalPages":1,"hasNext":false,"hasPrev":false} | ✅ PASS | — |
| `AM-API-002` | Asset Management | GET /assets/stats aggregates totals | API | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 200 with total, portfolioValue, avgHealth, byStatus[], byCategory[] | 200 {"total":2,"portfolioValue":120000,"avgHealth":100,"avgUtilization":3,"byStatus":[{"status":"Staging","count":2}],"byCategory":[{"category":"Compute","count":2, | ✅ PASS | — |
| `AM-SEC-001` | Asset Management | Unauthenticated GET /assets is rejected | Security | P0 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 401 with error envelope | 401 {"success":false,"error":{"code":"UNAUTHORIZED","message":"Missing bearer token"},"requestId":"4d346b9b-27a9-490c-916f-8 | ✅ PASS | — |
| `AM-SEC-002` | Asset Management | Malformed bearer token is rejected | Security | P0 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 401 | 401 | ✅ PASS | — |
| `AM-SEC-003` | Asset Management | JWT with invalid signature is rejected | Security | P0 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 401 | 401 | ✅ PASS | — |
| `AM-SEC-004` | Asset Management | Unauthenticated asset creation is rejected | Security | P0 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 401 and no asset written | 401 | ✅ PASS | — |
| `AM-API-010` | Asset Management | POST /assets creates an asset and mints an AST- id | Functional | P0 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 201, id matches /^AST-\d+$/ | 201 id=AST-157 | ✅ PASS | — |
| `AM-API-011` | Asset Management | Created asset is readable and healthStatus derived from score | Functional | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | healthScore 100 → healthStatus "Good"; custodian defaults to "Unassigned" | score=100 status=Good custodian=Unassigned | ✅ PASS | — |
| `AM-API-012` | Asset Management | Asset created without a serial stores empty, not a placeholder | Functional | P0 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | serialNumber === '' (field present, no INT-AST-… placeholder) | 201 serialNumber="" | ✅ PASS | — |
| `AM-API-013` | Asset Management | Multiple serial-less assets can coexist (partial unique index) | Database | P0 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 201 — empty serials are exempt from the unique index | 201  | ✅ PASS | — |
| `AM-DB-001` | Asset Management | Duplicate serial number is rejected | Database | P0 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 409 conflict | 409 A record with this serialNumber already exists | ✅ PASS | — |
| `AM-VAL-001` | Asset Management | Create validation — name missing | Validation | P0 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-VAL-002` | Asset Management | Create validation — name 1 char (below min 2) | Validation | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-BND-001` | Asset Management | Create validation — name exactly 2 chars (min boundary) | Boundary | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 201 | 201  | ✅ PASS | — |
| `AM-BND-002` | Asset Management | Create validation — name exactly 120 chars (max boundary) | Boundary | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 201 | 201  | ✅ PASS | — |
| `AM-BND-003` | Asset Management | Create validation — name 121 chars (above max) | Boundary | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-VAL-003` | Asset Management | Create validation — category missing | Validation | P0 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-VAL-004` | Asset Management | Create validation — category not in enum | Validation | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-VAL-005` | Asset Management | Create validation — location missing | Validation | P0 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-VAL-006` | Asset Management | Create validation — location.name empty string | Validation | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-VAL-007` | Asset Management | Create validation — serial 1 char (below min 2) | Validation | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-BND-004` | Asset Management | Create validation — serial exactly 64 chars (max boundary) | Boundary | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 201 | 201  | ✅ PASS | — |
| `AM-BND-005` | Asset Management | Create validation — serial 65 chars (above max) | Boundary | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-VAL-008` | Asset Management | Create validation — negative purchase price | Validation | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-BND-006` | Asset Management | Create validation — purchase price 0 (min boundary) | Boundary | P3 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 201 | 201  | ✅ PASS | — |
| `AM-VAL-009` | Asset Management | Create validation — healthScore above 100 | Validation | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-VAL-010` | Asset Management | Create validation — healthScore negative | Validation | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-BND-007` | Asset Management | Create validation — healthScore 0 (min boundary) | Boundary | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 201 | 201  | ✅ PASS | — |
| `AM-VAL-011` | Asset Management | Create validation — healthScore non-integer | Validation | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-VAL-012` | Asset Management | Create validation — status not in enum | Validation | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-VAL-013` | Asset Management | Create validation — criticality not in enum | Validation | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-VAL-014` | Asset Management | Create validation — explicit id in wrong format | Validation | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-BND-008` | Asset Management | Create validation — tags array of 20 (max boundary) | Boundary | P3 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 201 | 201  | ✅ PASS | — |
| `AM-BND-009` | Asset Management | Create validation — tags array of 21 (above max) | Boundary | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-VAL-015` | Asset Management | Create validation — utilization above 100 | Validation | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-VAL-016` | Asset Management | Create validation — purchasePrice as string | Validation | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-VAL-017` | Asset Management | Create validation — name of only whitespace | Validation | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | HTTP 422 | 422  | ✅ PASS | — |
| `AM-SEC-005` | Asset Management | NoSQL operator injected into a field is rejected | Security | P0 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 422 — object where a string is expected | 422 | ✅ PASS | — |
| `AM-SEC-006` | Asset Management | NoSQL operator in ?q= is treated as a literal, not an operator | Security | P0 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 200 and 0 results (escaped as literal text) | 200 count=0 | ✅ PASS | — |
| `AM-SEC-007` | Asset Management | HTML/script payload in name is stored verbatim (must be escaped on render) | Security | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | Stored as-is; React escapes on output. Recorded for the UI XSS check. | 201 stored="QA <img src=x onerror=alert(1)>" | ✅ PASS | — |
| `AM-NEG-001` | Asset Management | Malformed JSON body returns 400, not 500 | Negative | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 400 with an error envelope | 500 {"success":false,"error":{"code":"INTERNAL_ERROR","message":"Something went wrong","debug" | ❌ FAIL | High |
| `AM-SEC-008` | Asset Management | 2 MB payload is rejected (body size limit), not accepted | Security | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 400, 413 or 422 | 413 | ✅ PASS | — |
| `AM-NEG-002` | Asset Management | GET a non-existent asset returns 404 | Negative | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 404 with error envelope | 404 | ✅ PASS | — |
| `AM-SEC-009` | Asset Management | Path-traversal style id is rejected or 404, never a file read | Security | P0 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 400/404/422 — never a file read or 500 | 404 {"success":false,"error":{"code":"NOT_FOUND","message":"Asset not found"},"reque | ✅ PASS | — |
| `AM-API-014` | Asset Management | GET /assets/:id/profile returns asset + all timelines | API | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | asset, workOrders[], activity[], insights[], custody[] | 200 keys=asset,workOrders,activity,insights,custody | ✅ PASS | — |
| `AM-API-020` | Asset Management | PATCH with one field preserves every unsent field | Functional | P0 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | serial/price/health/name unchanged; status → Maintenance | status=Maintenance serial="QA-BASE-001" price=1000 health=100 | ✅ PASS | — |
| `AM-API-021` | Asset Management | PATCH can clear a serial number to empty | Functional | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 200 and serialNumber === '' | 200 serial="" | ✅ PASS | — |
| `AM-SEC-010` | Asset Management | PATCH cannot reassign the asset id | Security | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | id ignored or 400; original id still resolves | patch=200 original-still-exists=true | ✅ PASS | — |
| `AM-NEG-003` | Asset Management | PATCH a non-existent asset returns 404 | Negative | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 404 | 404 | ✅ PASS | — |
| `AM-DB-002` | Asset Management | healthStatus is re-derived when healthScore changes | Database | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | healthScore 10 → healthStatus "Critical" | score=10 status=Critical | ✅ PASS | — |
| `AM-API-030` | Asset Management | POST /assets/bulk applies a patch across a selection | Functional | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 200 with updated.length === 3 | 200 updated=3 failed=0 | ✅ PASS | — |
| `AM-API-031` | Asset Management | Bulk update reports partial success rather than failing whole batch | Functional | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 200, 1 updated, 1 failed with a reason | 200 updated=1 failed=[{"id":"AST-99999999","reason":"Asset not found"}] | ✅ PASS | — |
| `AM-VAL-020` | Asset Management | Bulk update with an empty id list is rejected | Validation | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 422 — min 1 id | 422 | ✅ PASS | — |
| `AM-VAL-021` | Asset Management | Bulk update with an empty patch is rejected | Validation | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 422 — "Provide at least one field to change" | 422 "Request validation failed" | ✅ PASS | — |
| `AM-BND-010` | Asset Management | Bulk update above the 500-id cap is rejected | Boundary | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 422 — max 500 ids | 422 | ✅ PASS | — |
| `AM-SEC-011` | Asset Management | Bulk patch refuses per-asset fields (serialNumber) not in its allowlist | Security | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 422 — serialNumber is not a bulk-editable field | 422 | ✅ PASS | — |
| `AM-API-040` | Asset Management | Status filter returns only matching assets | Functional | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | every row has status Maintenance | 200 n=1 allMatch=true | ✅ PASS | — |
| `AM-API-041` | Asset Management | CSV multi-value category filter works | Functional | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | only Compute or Storage rows | 200 n=12 allMatch=true | ✅ PASS | — |
| `AM-VAL-030` | Asset Management | Unknown category filter value | Validation | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 422, or 200 with 0 results — never a 500 | 200 n=0 | ✅ PASS | — |
| `AM-API-042` | Asset Management | Pagination honours limit and reports meta | Functional | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | at most 2 rows; meta.limit === 2 | 200 n=2 meta={"page":1,"limit":2,"total":12,"totalPages":6,"hasNext":true,"hasPrev":false} | ✅ PASS | — |
| `AM-BND-011` | Asset Management | Excessive page limit is capped or rejected (DoS guard) | Boundary | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 422 (documented max 200), or 200 with a capped limit | 422 limit=undefined n=undefined | ✅ PASS | — |
| `AM-BND-012` | Asset Management | Negative page limit is rejected or normalised | Boundary | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 422, or 200 with a sane limit | 422 limit=undefined | ✅ PASS | — |
| `AM-SEC-012` | Asset Management | Sort parameter is allowlisted (no arbitrary field injection) | Security | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 422, or 200 using the default sort | 200 | ✅ PASS | — |
| `AM-API-043` | Asset Management | Free-text search finds an asset by name | Functional | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | at least 1 result containing the created asset | 200 n=1 | ✅ PASS | — |
| `AM-API-044` | Asset Management | Partial serial-number search matches (regex, not $text) | Functional | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | ≥1 result for a partial serial | 200 n=1 | ✅ PASS | — |
| `AM-SEC-013` | Asset Management | Regex metacharacters in search are escaped (no ReDoS / crash) | Security | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 200 — not a 500 from an invalid regex | 200 {"success":true,"data":[],"meta":{"page":1,"limit":25,"total":0,"totalPages":0," | ✅ PASS | — |
| `AM-PERF-001` | Asset Management | Asset list responds within 2000 ms | Performance | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | < 2000 ms | 245 ms | ✅ PASS | — |
| `AM-PERF-002` | Asset Management | 10 concurrent list requests complete within 10 s | Performance | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | < 10000 ms for 10 concurrent reads | 2363 ms | ✅ PASS | — |
| `AM-API-050` | Asset Management | DELETE retires an asset; it stops resolving | Functional | P1 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 204 then 404 on read-back | delete=204 read=404 | ✅ PASS | — |
| `AM-NEG-004` | Asset Management | DELETE a non-existent asset returns 404 | Negative | P2 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | 404 | 404 | ✅ PASS | — |
| `AR-CAT-001` | Asset Management | GET /registration/catalog returns sectioned field catalogue | API | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | sections identity, assignment, location, technical, commercial, maintenance, tracking | 200 sections=["identity","assignment","location","technical","commercial","maintenance","tracking"] | ✅ PASS | — |
| `AR-CAT-002` | Asset Management | Criticality is absent from the registration catalogue | Functional | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | no "criticality" field — it is no longer asked during registration | criticality present = false | ✅ PASS | — |
| `AR-CAT-003` | Asset Management | Catalogue declares core and identity field sets | API | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | coreFields includes name/facilityId; identityFields includes serialNumber/macAddress | core=["name","facilityId"] identity=["serialNumber","macAddress","imei"] | ✅ PASS | — |
| `AR-CAT-005` | Asset Management | Template editor is not offered the fields the server mints | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | assetTag and trackingId absent — a template cannot ask for a number it does not own | assetTag=false trackingId=false | ✅ PASS | — |
| `AR-CAT-006` | Asset Management | Template editor is not offered category — the template sets it | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | category absent from the pickable catalogue and from coreFields | field=false core=false | ✅ PASS | — |
| `AR-CAT-004` | Asset Management | Employee ID exists as a first-class assignment field | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | custodianEmployeeId in the assignment section | present=true | ✅ PASS | — |
| `AR-DEF-001` | Asset Management | GET /registration/defaults derives site from the caller scope | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | location + facilities[] + registeredBy, derived from the user homeScopeId | 200 location={"id":"FAC-1","name":"Hyderabad warehouse"} facilities=1 by=Raj | ✅ PASS | — |
| `AR-FRM-001` | Asset Management | Blank form offers the whole catalogue, requiring only core fields | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | many fields; required set == core set (name, category, facilityId) | 200 fields=31 required=["name","category","facilityId"] | ✅ PASS | — |
| `AR-FRM-005` | Asset Management | Blank form never asks for the numbers the server mints | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | no assetTag or trackingId input on the blank form | assetTag=false trackingId=false | ✅ PASS | — |
| `AR-FRM-006` | Asset Management | Minted fields are declared so the review step can name them | API | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | derived[] carries assetTag and trackingId with origin=generated and a note | derived=["assetTag:generated","trackingId:generated"] | ✅ PASS | — |
| `AR-VAL-001` | Asset Management | Empty draft reports each missing core field, per section | Validation | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | valid=false; errors for name, category, facilityId; sections carry their own errors | 200 valid=false errors=["name","category","facilityId"] | ✅ PASS | — |
| `AR-VAL-002` | Asset Management | Custodian named without an employee ID is rejected | Validation | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | error on custodianEmployeeId | valid=false errors=["custodianEmployeeId"] | ✅ PASS | — |
| `AR-VAL-003` | Asset Management | Employee ID without a name is rejected (half-filled assignment) | Validation | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | error on custodianName | valid=false errors=["custodianName"] | ✅ PASS | — |
| `AR-VAL-004` | Asset Management | Unassigned asset is valid — assignment is optional | Validation | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | valid=true with no custodian at all | valid=true errors=[] | ✅ PASS | — |
| `AR-VAL-005` | Asset Management | Maintenance contract switched on requires a provider | Validation | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | error on amcProvider | errors=["amcProvider"] | ✅ PASS | — |
| `AR-VAL-006` | Asset Management | Maintenance section is skippable when there is no contract | Validation | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | valid=true — most assets have no contract | valid=true errors=[] | ✅ PASS | — |
| `AR-VAL-007` | Asset Management | A client-supplied tag ID is ignored, not trusted | Security | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | valid=true and no trackingId error — the field is not asked for and not accepted | valid=true errors=[] | ✅ PASS | — |
| `AR-VAL-008` | Asset Management | Malformed MAC address is rejected with a readable hint | Validation | P2 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | error on macAddress naming the expected format | errors=[{"key":"macAddress","section":"technical","message":"Six pairs of hex digits, e.g. 00:1B:44:11:3A:B7"}] | ✅ PASS | — |
| `AR-VAL-009` | Asset Management | One-character serial rejected; blank still allowed | Validation | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | error on serialNumber | errors=["serialNumber"] | ✅ PASS | — |
| `AR-VAL-010` | Asset Management | Unknown site is refused at commit | Validation | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 422 on register | validate=200 register=422 | ✅ PASS | — |
| `AR-VAL-011` | Asset Management | Retired sources (po/scan/erp/adopt) are refused | Validation | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 422 — only blank, template, clone, import remain | 422 | ✅ PASS | — |
| `AR-VAL-012` | Asset Management | Template source without a templateId is refused | Validation | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 422 | 422 | ✅ PASS | — |
| `AR-REG-001` | Asset Management | Blank registration creates an asset | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 201 with an AST- id | 201 id=AST-143  | ✅ PASS | — |
| `AR-REG-002` | Asset Management | Registration writes custodian + employee ID to the asset | Database | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | custodian "Anita Rao", onboarding.custodianEmployeeId "BCSS-2291" | custodian=Anita Rao employeeId=BCSS-2291 | ✅ PASS | — |
| `AR-REG-003` | Asset Management | Maintenance contract persists in its own sub-document | Database | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | onboarding.maintenance { hasContract:true, provider:"Dell ProSupport" } | {"hasContract":true,"provider":"Dell ProSupport","reference":"AMC-99"} | ✅ PASS | — |
| `AR-REG-004` | Asset Management | No serial number is invented when none was given | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | serialNumber === '' | serialNumber="" | ✅ PASS | — |
| `AR-REG-005` | Asset Management | Site resolves to a named location, not just an id | Database | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | location.id and location.name both populated from the scope tree | {"id":"FAC-1","name":"Hyderabad warehouse","zone":"Desk 14"} | ✅ PASS | — |
| `AR-REG-006` | Asset Management | Registration is Active with no approval gate | Functional | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | onboarding.state === "Active" — the empty approval step is gone | state=Active source=blank | ✅ PASS | — |
| `AR-REG-007` | Asset Management | Blank registration stores no asset class | Database | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | onboarding.classId === '' — classify later, do not block now | classId="" | ✅ PASS | — |
| `AR-REG-008` | Asset Management | Unassigned registration stores custodian "Unassigned" | Database | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 201, custodian "Unassigned" | 201 custodian=Unassigned | ✅ PASS | — |
| `AR-REG-009` | Asset Management | Commit is refused when a core field is missing | Negative | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 422 naming category | 422 details=["category"] | ✅ PASS | — |
| `AR-DB-001` | Asset Management | Every registration is issued its own asset tag | Database | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | both 201, both tagged AG-<n>, and the two tags differ | first=201/AG-4 second=201/AG-5 | ✅ PASS | — |
| `AR-DB-002` | Asset Management | A client-supplied asset tag cannot override the minted one | Security | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 201 with a minted AG- tag, not "QA-TAG-001" | 201 assetTag=AG-6 | ✅ PASS | — |
| `AR-DB-003` | Asset Management | Choosing a tag type mints a tag ID carrying that prefix | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | trackingId matches QR-<n>, trackingIntent "bound" | 201 trackingId=QR-1 intent=bound | ✅ PASS | — |
| `AR-DB-004` | Asset Management | No tag type means no tag ID is invented | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | trackingId absent/empty, trackingIntent "not-tracked" | 201 trackingId=undefined intent=not-tracked | ✅ PASS | — |
| `AR-TPL-001` | Asset Management | Create a template selecting catalogue + custom fields | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 201 with a TPL- id | 201 id=TPL-16  | ✅ PASS | — |
| `AR-TPL-002` | Asset Management | Template referencing an unknown field is rejected | Validation | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 422 — the field resolves to nothing | 422 | ✅ PASS | — |
| `AR-TPL-003` | Asset Management | Template selecting the same field twice is rejected | Validation | P2 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 422 | 422 | ✅ PASS | — |
| `AR-TPL-004` | Asset Management | Custom field may not shadow a catalogue field | Validation | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 422 — two paths under one name | 422 | ✅ PASS | — |
| `AR-TPL-005` | Asset Management | Custom dropdown with no options is rejected | Validation | P2 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 422 | 422 | ✅ PASS | — |
| `AR-TPL-006` | Asset Management | Template form asks only the chosen fields, with the author required flags | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | the 9 chosen fields less the category the template answers itself = 8; serialNumber and macAddress required; model optional | n=8 required=["name","facilityId","serialNumber","manufacturer","macAddress"] | ✅ PASS | — |
| `AR-TPL-017` | Asset Management | A template answers the category instead of asking for it | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | no category input; derived carries category=Compute with origin "template" | asked=false derived={"key":"category","label":"Category","section":"identity","origin":"template","value":"Compute","note":"Set by the QA Computer Template template."} | ✅ PASS | — |
| `AR-TPL-007` | Asset Management | Custom template field appears in the resolved form | Functional | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | warrantyAgent present, mapped into onboarding.attributes | present=true path=onboarding.attributes.warrantyAgent | ✅ PASS | — |
| `AR-TPL-008` | Asset Management | Template-mandated fields are enforced at validation | Validation | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | errors for serialNumber, manufacturer, macAddress | errors=["serialNumber","manufacturer","macAddress"] | ✅ PASS | — |
| `AR-TPL-009` | Asset Management | Registering from a template creates the asset | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 201 | 201  | ✅ PASS | — |
| `AR-TPL-010` | Asset Management | Template asset stores templateId and custom attributes | Database | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | onboarding.templateId=TPL-16; attributes.macAddress + attributes.warrantyAgent persisted | templateId=TPL-16 attrs={"macAddress":"00:1B:44:11:3A:B7","processor":"Intel i7-1355U","warrantyAgent":"Redington"} | ✅ PASS | — |
| `AR-TPL-018` | Asset Management | The template's category wins over anything the caller sends | Security | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | category "Compute" from the template, not the "Furniture" that was posted | category=Compute | ✅ PASS | — |
| `AR-TPL-011` | Asset Management | Template usage counter increments on use | Functional | P2 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | usageCount === 1 | usageCount=1 | ✅ PASS | — |
| `AR-TPL-012` | Asset Management | Template list returns active templates | API | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 200 with our template present | 200 n=2 | ✅ PASS | — |
| `AR-TPL-013` | Asset Management | Template update preserves unsent fields | Functional | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | description changed; fields[] and name intact | 200 desc=Updated by QA fields=9 name=QA Computer Template | ✅ PASS | — |
| `AR-TPL-014` | Asset Management | Deleting a template archives it rather than destroying it | Functional | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | status archived, still readable (assets reference it) | 200 status=archived | ✅ PASS | — |
| `AR-TPL-015` | Asset Management | Archived template cannot be used for new assets | Negative | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 409 conflict | 409 | ✅ PASS | — |
| `AR-TPL-016` | Asset Management | Unknown template returns 404 | Negative | P2 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 404 | 404 | ✅ PASS | — |
| `AR-CLN-001` | Asset Management | Clone prefill copies shared values | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | manufacturer/model/category copied from the source asset | 200 manufacturer=Dell model=Latitude 5440 category=Compute | ✅ PASS | — |
| `AR-CLN-002` | Asset Management | Clone clears every identity field the user must type, and names them | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | serialNumber, macAddress, imei blanked and listed; the minted assetTag and trackingId are not asked for at all | cleared=["serialNumber","macAddress","imei"] serial="" mac="" | ✅ PASS | — |
| `AR-CLN-003` | Asset Management | Clone starts unassigned rather than inheriting the custodian | Functional | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | custodianName === '' — a new unit is not automatically that person's | custodianName="" | ✅ PASS | — |
| `AR-CLN-004` | Asset Management | Registering a clone creates a distinct asset | Functional | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 201 with a new id and the typed serial | 201 id=AST-151 serial=QA-CLONE-SER-9  | ✅ PASS | — |
| `AR-CLN-008` | Asset Management | A clone is issued its own asset tag, not the original one | Database | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | clone assetTag is an AG- number and differs from the source asset tag | clone=AG-10 source=AG-9 | ✅ PASS | — |
| `AR-CLN-005` | Asset Management | Clone records its provenance | Database | P2 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | onboarding.clonedFromId === AST-150 | clonedFromId=AST-150 source=clone | ✅ PASS | — |
| `AR-CLN-006` | Asset Management | Clone reusing the source serial is rejected | Database | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 409 — two assets cannot claim one serial | 409 | ✅ PASS | — |
| `AR-CLN-007` | Asset Management | Cloning an unknown asset returns 404 | Negative | P2 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 404 | 404 | ✅ PASS | — |
| `AR-SEC-001` | Asset Management | Unauthenticated registration is rejected | Security | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 401 | 401 | ✅ PASS | — |
| `AR-SEC-002` | Asset Management | Field catalogue is not public | Security | P1 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | 401 | 401 | ✅ PASS | — |
| `AR-SEC-003` | Asset Management | Values for unknown keys are ignored, not written through | Security | P0 | Authenticate → call the registration/template endpoint → assert on status, envelope and the stored document | id minted normally; healthScore untouched at 100 | 201 id=AST-153 healthScore=100 | ✅ PASS | — |
| `ARU-001` | Asset Management | Source picker offers exactly the four remaining sources | UI | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | Add an asset · From a template · Clone an existing asset · Bulk import | found=4/4 | ✅ PASS | — |
| `ARU-002` | Asset Management | Retired sources are gone from the picker | UI | P1 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | no Purchase Order, Scan, ERP or Adopt cards | po=false scan=false erp=false adopt=false | ✅ PASS | — |
| `ARU-003` | Asset Management | Source picker renders with no console errors | UI | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | 0 console errors | 0 errors | ✅ PASS | — |
| `ARU-010` | Asset Management | Add-asset opens as a wizard on step 1 of 8 | UI | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | "Step 1 of 8" and the identity section heading | STEP 1 OF 8 | ✅ PASS | — |
| `ARU-011` | Asset Management | Only the current step is rendered, not the whole form | UX | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | identity fields present; assignment/commercial fields absent | fields=["name","category","manufacturer","model","serialNumber"] | ✅ PASS | — |
| `ARU-012` | Asset Management | Rail lists every step with optional ones marked | UX | P1 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | Review & register step present; optional steps flagged | review=true optMarkers=4 | ✅ PASS | — |
| `ARU-013` | Asset Management | Criticality and asset class are not asked anywhere | Functional | P1 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | no criticality field, no asset-class field | criticality=false assetClass=false | ✅ PASS | — |
| `ARU-014` | Asset Management | Step 1 does not ask anyone to invent an asset tag | Functional | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | no asset-tag input — the number is issued by the server | assetTagInput=false | ✅ PASS | — |
| `ARU-015` | Asset Management | Wizard loads with no console errors | UI | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | 0 console errors | 0 errors | ✅ PASS | — |
| `ARU-020` | Asset Management | Next advances to the assignment step | Functional | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | Step 2 of 8, custodian fields present | STEP 2 OF 8 fields=["custodianName","custodianEmployeeId","department"] | ✅ PASS | — |
| `ARU-021` | Asset Management | Custodian without an employee ID raises a visible error | Validation | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | an inline message asking for the employee ID | An employee ID is required when the asset is assigned to someone. | ✅ PASS | — |
| `ARU-022` | Asset Management | The rail marks the step that has the error | UX | P1 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | a "!" marker on the assignment step | railShowsError=true | ✅ PASS | — |
| `ARU-030` | Asset Management | Review step summarises what will be saved | UX | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | the entered name and custodian shown back, with Edit links | name=true custodian=true edit=true | ✅ PASS | — |
| `ARU-031` | Asset Management | Register appears only on the review step and is enabled when valid | Functional | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | button present, enabled, "Ready to register" | {"present":true,"disabled":false,"ready":true} | ✅ PASS | — |
| `ARU-034` | Asset Management | Review is one card per section, not a single stacked sheet | UX | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | a separate card for each section that has values, plus the derived ones | cards=["What is it?","Who holds it?","Where is it?","Issued for you","Left blank"] | ✅ PASS | — |
| `ARU-035` | Asset Management | Review names the numbers the server will issue | UX | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | "Issued for you" card naming the asset tag, so a blank field is not read as a missing one | card=true tag=true onSave=true | ✅ PASS | — |
| `ARU-036` | Asset Management | Skipped sections are gathered into one card, not repeated | UX | P1 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | a "Left blank" card instead of "Nothing entered" once per empty section | leftBlank=true nothingEntered=0 | ✅ PASS | — |
| `ARU-032` | Asset Management | Registering from the wizard creates the asset | Functional | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | lands on /assets/AST-… | url=/assets/AST-156 | ✅ PASS | — |
| `ARU-033` | Asset Management | Wizard-registered asset is stored correctly | Database | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | name, custodian, employee ID and a resolved site; serial empty | name=QA Wizard Laptop custodian=Anita Rao emp=BCSS-2291 site=Hyderabad warehouse serial="" | ✅ PASS | — |
| `ARU-037` | Asset Management | The asset the wizard created carries a minted asset tag | Database | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | onboarding.assetTag matches AG-<n>, without anyone having typed it | assetTag=AG-15 | ✅ PASS | — |
| `ARU-040` | Asset Management | Template editor opens as a wizard | UI | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | Step 1 of 8 on the template details step | STEP 1 OF 8 | ✅ PASS | — |
| `ARU-041` | Asset Management | Icon is chosen from a grid, not typed | UX | P1 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | a radiogroup of 20+ IT icons | {"present":true,"count":23} | ✅ PASS | — |
| `ARU-042` | Asset Management | Template editor loads with no console errors | UI | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | 0 console errors | 0 errors | ✅ PASS | — |
| `ARU-043` | Asset Management | Next moves to the first field section, one section per step | UX | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | Step 2 of 8 showing the identity fields with Mandatory toggles | STEP 2 OF 8 mandatory=6 | ✅ PASS | — |
| `ARU-044` | Asset Management | Core fields are pre-ticked and cannot be removed | Functional | P1 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | Asset name checked and disabled | {"checked":true,"disabled":true} | ✅ PASS | — |
| `ARU-047` | Asset Management | The template editor does not re-offer questions it already answers | UX | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | no Category tick-box and no Asset tag tick-box on the identity step | category=false assetTag=true | ✅ PASS | — |
| `ARU-045` | Asset Management | Saving from the last step creates the template | Functional | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | navigates to /assets/templates with the new template listed | url=/assets/templates listed=true | ✅ PASS | — |
| `ARU-046` | Asset Management | Wizard-authored template stores fields, icon and required flags | Database | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | serialNumber required; icon 💻 | icon=💻 fields=2 required=["serialNumber"] | ✅ PASS | — |
| `ARU-050` | Asset Management | Template registration asks only the chosen fields | Functional | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | identity step shows serial and manufacturer; no purchase price on it | step=STEP 1 OF 3 fields=["name","serialNumber","manufacturer"] | ✅ PASS | — |
| `ARU-060` | Asset Management | Clone chooser lists assets with a search box | UI | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | search input plus at least one asset row | searchInput=true rows=true | ✅ PASS | — |
| `ARU-061` | Asset Management | Clone wizard warns which fields could not be copied | UX | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | a callout naming the identity fields left empty | 3 fields could not be copied | ✅ PASS | — |
| `ARU-062` | Asset Management | Clone copies shared values but blanks the identity fields | Functional | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | serial empty, name carried over | {"serial":"","name":"QA Wizard Laptop"} | ✅ PASS | — |
| `ARU-063` | Asset Management | Clone flow renders with no console errors | UI | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | 0 console errors | 0 errors | ✅ PASS | — |
| `ARU-070` | Asset Management | Asset Classes is gone from the navigation | Functional | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | no "Asset Class" row; "Asset Templates" present instead | classes=false templates=true | ✅ PASS | — |
| `ARU-071` | Asset Management | The asset-classes API no longer exists | API | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | 404 | 404 | ✅ PASS | — |
| `ARU-072` | Asset Management | The Administration class screen no longer resolves | Functional | P0 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | a not-found state, not the class editor | Skip to content / 📊 / Dashboard / ▾ / 🗺️ / Asset Tracking / ▾ / ✨ / AI Asset Intelligence / ▾ / 🪪 / Asset Man | ✅ PASS | — |
| `ARU-080` | Asset Management | Add-asset wizard at mobile 390 does not scroll horizontally | Responsive | P2 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | scrollWidth <= viewport width | scrollWidth=390 viewport=390 | ✅ PASS | — |
| `ARU-081` | Asset Management | Add-asset wizard at desktop 1440 does not scroll horizontally | Responsive | P2 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | scrollWidth <= viewport width | scrollWidth=1440 viewport=1440 | ✅ PASS | — |
| `ARU-090` | Asset Management | Every wizard input is programmatically labelled | Accessibility | P1 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | 0 unlabelled inputs | 0 of 5 unlabelled | ✅ PASS | — |
| `ARU-091` | Asset Management | The active step is exposed with aria-current | Accessibility | P1 | Sign in → open the add-asset step → wait for the data gate → assert on rendered DOM, console and the stored document | exactly one aria-current="step" | 1 marked | ✅ PASS | — |
| `AM-UI-001` | Asset Management | Login page renders with the sign-in form | UI | P0 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | "Sign in" heading and email/password inputs visible | rendered=true | ✅ PASS | — |
| `AM-UI-002` | Asset Management | Valid credentials sign in and land on the workspace | Functional | P0 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | Navigates away from /auth/login into the app shell | url=/ loggedIn=true | ✅ PASS | — |
| `AM-UI-010` | Asset Management | Asset Registry page renders its header and toolbar | UI | P0 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | "Asset Registry" title, Export CSV and + Add Asset actions | title=true export=true add=true | ✅ PASS | — |
| `AM-UX-001` | Asset Management | Registry shows triage lens chips with live queue counts | UX | P1 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | All Assets + the exception queues rendered as filter chips | chips present: ["All Assets","Setup incomplete","Untracked","Unassigned","Needs attention"] | ✅ PASS | — |
| `AM-UI-011` | Asset Management | Registry page loads with no console errors | UI | P0 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | 0 console errors / uncaught exceptions | 0 errors | ✅ PASS | — |
| `AM-API-060` | Asset Management | Registry page issues no failing API calls | API | P0 | Authenticate → issue the HTTP request described by the feature → assert on status code and response envelope | no /api/ response with status >= 400 | none | ✅ PASS | — |
| `AM-PERF-010` | Asset Management | Workspace becomes interactive within 3 s on an empty estate | Performance | P1 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | < 3000 ms from navigation to the registry being usable | 4994 ms (blocked behind GET /dataset) | ❌ FAIL | High |
| `AM-UI-020` | Asset Management | /assets/new renders the registration surface | UI | P0 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | A form or source picker appears | Skip to content / 📊 / Dashboard / ▾ / 🗺️ / Asset Tracking / ▾ / ✨ / AI Asset Intelligence / ▾ / 🪪 / Asset Management / ▾ / IT Asset Registry / Add Asset / Asset Templates / Label  | ✅ PASS | — |
| `AM-UI-021` | Asset Management | /assets/new loads with no console errors | UI | P0 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | 0 console errors | 0 errors | ✅ PASS | — |
| `AM-A11Y-001` | Asset Management | Form inputs are programmatically labelled | Accessibility | P1 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | every input has a <label for>, aria-label or wrapping label | 0/0 labelled; unlabelled: [] | ✅ PASS | — |
| `AM-UI-030` | Asset Management | API-created asset with no onboarding record renders in the registry | Functional | P0 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | Row appears; page does not crash on the optional onboarding field | visible=true errors=0 | ✅ PASS | — |
| `AM-SEC-020` | Asset Management | Script payload in an asset name does not execute (stored XSS) | Security | P0 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | window.__xss undefined — React escapes the name as text | xssExecuted=false | ✅ PASS | — |
| `AM-UI-031` | Asset Management | Registry with data loads with no console errors | UI | P0 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | 0 console errors | 0 errors | ✅ PASS | — |
| `AM-UI-040` | Asset Management | Registry search narrows the table to matching rows | Functional | P1 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | Matching asset visible; non-matching hidden | minimalVisible=true | ✅ PASS | — |
| `AM-UX-002` | Asset Management | Search with no matches shows a no-results state with a way out | UX | P1 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | "No assets match your filters" plus a Clear filters action | noResults=true clearAction=true | ✅ PASS | — |
| `AM-UX-003` | Asset Management | Filter state is reflected in the URL (shareable view) | UX | P2 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | ?q=QA present in the query string | search=?q=QA | ✅ PASS | — |
| `AM-UI-050` | Asset Management | Asset 360 detail page opens for a minimal asset | Functional | P0 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | Asset name shown; no crash on absent onboarding/telemetry | nameShown=true errors=0 first= | ✅ PASS | — |
| `AM-NEG-010` | Asset Management | Unknown asset id shows a not-found state, not a blank page | Negative | P1 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | A "not found" message with a way back | text=Skip to content / 📊 / Dashboard / ▾ / 🗺️ / Asset Tracking / ▾ / ✨ / AI Asset Intelligence / ▾ / 🪪 / Asset Management / ▾ / IT Asset Registry / Ad errors=0 | ✅ PASS | — |
| `AM-RSP-001` | Asset Management | Registry at mobile 390×844 does not scroll horizontally | Responsive | P2 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | document scrollWidth <= viewport width | scrollWidth=390 viewport=390 | ✅ PASS | — |
| `AM-RSP-002` | Asset Management | Registry at tablet 768×1024 does not scroll horizontally | Responsive | P2 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | document scrollWidth <= viewport width | scrollWidth=768 viewport=768 | ✅ PASS | — |
| `AM-RSP-003` | Asset Management | Registry at desktop 1440×900 does not scroll horizontally | Responsive | P2 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | document scrollWidth <= viewport width | scrollWidth=1440 viewport=1440 | ✅ PASS | — |
| `AM-A11Y-002` | Asset Management | All images carry an alt attribute | Accessibility | P2 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | 0 images without alt | 0 of 2 missing alt | ✅ PASS | — |
| `AM-A11Y-003` | Asset Management | Icon-only buttons expose an accessible name | Accessibility | P1 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | 0 buttons without text, aria-label or title | 0 of 33 buttons unnamed | ✅ PASS | — |
| `AM-A11Y-004` | Asset Management | Row-selection checkboxes are labelled for screen readers | Accessibility | P1 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | 0 unlabelled checkboxes | 0 of 5 checkboxes unlabelled | ✅ PASS | — |
| `AM-A11Y-005` | Asset Management | Page exposes exactly one H1 landmark | Accessibility | P2 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | exactly 1 <h1> | 1 h1 elements | ✅ PASS | — |
| `AM-A11Y-006` | Asset Management | Data table uses <th> header cells | Accessibility | P2 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | every table has header cells | 0 of 1 tables without <th> | ✅ PASS | — |
| `AM-A11Y-007` | Asset Management | Registry exposes keyboard-focusable controls | Accessibility | P2 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | > 5 focusable elements | 68 focusable | ✅ PASS | — |
| `AM-A11Y-008` | Asset Management | Sortable column headers are keyboard operable | Accessibility | P2 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | every clickable header is focusable (tabindex/button/role) | 0 of 4 sortable headers keyboard-reachable | ❌ FAIL | Medium |
| `AM-PERM-001` | Asset Management | Assets route is guarded when the session is cleared | Permission | P0 | Sign in → navigate to the screen under test → wait for the data gate → assert on rendered DOM, console and network | Redirect to /auth/login, or an explicit signed-out state — never the registry with data | path=/login showsRegistryRows=false | ✅ PASS | — |
